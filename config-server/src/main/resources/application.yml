spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:manjunath-45/ibm-config-server.git
          clone-on-start: true
          force-pull: true
server:
  port: 9090